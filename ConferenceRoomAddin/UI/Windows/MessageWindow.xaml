<Window x:Class="ConferenceRoomAddin.UI.Windows.MessageWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="560" Height="150" WindowStartupLocation="CenterScreen" Background="#666" WindowStyle="None" AllowsTransparency="True">
    <Border BorderBrush="#F2BB46" BorderThickness="6" Margin="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="1" Text="{Binding MainMessage}" FontSize="36" Foreground="White" HorizontalAlignment="Center" Margin="0,4"/>
            <TextBlock Grid.Row="2" Text="{Binding SubMessage}" FontSize="14" Foreground="White" HorizontalAlignment="Center" Margin="0,4"/>
        </Grid>
    </Border>
</Window>
